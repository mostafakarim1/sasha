export class Airport {
  country: {
    code: string;
    name: string;
  };

  city: {
    code: string;
    name: string;
  };

  terminal: {
    code: string;
  };

  airport: {
    code: string;
    name: string;
  };
}
